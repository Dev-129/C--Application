 #include<stdio.h>
 int main()
 {
	 auto A=11;
	 auto char CH= 'dipss';
	 printf("the value of A=%d",A);
	 printf(" the value of CH=%c",CH);
	 printf(" the add of A=%u",&A);
	 printf("the add of CH=%U",&CH);
	 printf(" the size of A=%d",sizeof(A);
	 printf("the size of CH=%d",sizeof(CH);
	 return 0;
 }